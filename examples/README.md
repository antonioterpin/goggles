# Goggles Examples

Small, self-contained scripts showing the new API.

> Importantly: importing `goggles` has **no side effects**. Only `run(...)` attaches handlers.

## Quick start

```bash
# run any example
python examples/01_basic_run.py
